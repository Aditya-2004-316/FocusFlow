# FocusFlow Backend Environment Variables

# --- REQUIRED FOR ALL MODES ---
PORT=5000
MONGODB_URI=mongodb://localhost:27017/focusflow
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# --- PRODUCTION ONLY ---
# Set this to 'production' on Render
NODE_ENV=development

# The URL of your deployed frontend (e.g., https://focusflow.vercel.app)
# You can provide multiple origins separated by commas
CORS_ORIGIN_PROD=http://localhost:5173

# Database connection for production (fallback to MONGODB_URI if not set)
MONGODB_URI_PROD=

# --- OPTIONAL / ADVANCED ---
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
FRONTEND_URL=http://localhost:5173
